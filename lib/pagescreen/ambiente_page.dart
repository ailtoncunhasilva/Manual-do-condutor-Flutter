import 'package:flutter/material.dart';

class AmbientePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 6,
        title: Text('Meio Ambiente'),
        centerTitle: true,
      ),
    );
  }
}
