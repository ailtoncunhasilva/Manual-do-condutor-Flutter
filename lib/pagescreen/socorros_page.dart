import 'package:flutter/material.dart';

class SocorrosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 6,
        title: Text('Primeiros Socorros'),
        centerTitle: true,
      ),
    );
  }
}