import 'package:flutter/material.dart';

class MecanicaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 6,
        title: Text('Mecânica Básica'),
        centerTitle: true,
      ),
    );
  }
}