import 'package:flutter/material.dart';

class DefensivaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 6,
        title: Text('Direção Defensiva'),
        centerTitle: true,
      ),
    );
  }
}